# Override .gitignore: force-include the three data artifacts the Dockerfile
# COPYs at build time. Negations come BEFORE later excludes so they stick.
!data/
!data/verses.parquet
!data/verse_embeddings.npy
!data/graph.json

# VCS / editor / OS
.git/
.gitignore
.gitattributes
.idea/
.vscode/
.claude/
.DS_Store

# Local secrets — set OPENAI_API_KEY in Railway dashboard, not here.
.env
.env.*
!.env.example

# Python cruft
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Ingestion + source corpus aren't needed at runtime.
bible_ingest.py
kjv.txt
data/extractions/

# Streamlit prototype lives outside the API container.
app.py

# Next.js frontend deploys separately to Vercel.
frontend/

# Build/test cruft
.pytest_cache/
.mypy_cache/
*.log
